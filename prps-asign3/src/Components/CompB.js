


const ComponB=(props)=>{
    return(
               <div>

                <h2>Passing Props- {props.name}{props.age} count:{props.count}</h2>
        </div>
    )

}

export default ComponB;